import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'schnitzeljagd_steven_yves',
  webDir: 'www'
};

export default config;
